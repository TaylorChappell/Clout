window.CLOUT_CONFIG = {
  // Optional runtime override for direct-upload deployments.
  // Create a free project at https://cloud.reown.com and paste its Project ID here.
  walletConnectProjectId: "",
};
